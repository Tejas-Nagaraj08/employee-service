spring:
  application:
    name: tj-emp-dl

  datasource:
    url: jdbc:postgresql://localhost:5432/employee
    username: postgres
    password: tj08

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true